const path = require('path');

module.exports = {
    models_tables: path.resolve('src', 'database', 'models_tables'),
    config: path.resolve('src', 'database', 'config'),
    migrations: path.resolve('src', 'database', 'migrations')
};
